/* 

DO NOT CHANGE THIS FILE

*/

require("dotenv").config()



// maybe use the below function here??  :
// describe("attachActivitiesToRoutines(routinesArray)", () => {
//   it("it is passed an empty routinesArray, then the function should return a new array", async () => {
//     const routinesArray = []
//     const updatedArray = attachActivitiesToRoutines(routinesArray)

//     expect(updatedArray).toBeInstanceOf(Array);
//     expect(updatedArray).not.toEqual(routinesArray);
//   });

//   it("should loop over the array of routines and attach to each routine object an array of the activities that belong to it", async () => {
//     const routinesArray = []
//     const updatedArray = attachActivitiesToRoutines(routinesArray)

//     expect(updatedArray).toBeInstanceOf(Array);
//     expect(updatedArray).not.toEqual(routinesArray);
//   });
// });